#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
AskQet — принятая оснастка одним источником.

Заводится не для красоты, а по находке сводной сверки. В проекте
одновременно жили две палитры: краски из palette_v2 (#514F4A по #F9F3ED) и
принятая оснастка из premium.json (#392B1E по #F4EDE2). Тридцать четыре
модуля брали первую, семь — вторую, и граница проходила не по смыслу, а по
времени: всё, что делалось до доводки оснастки, осталось на старой краске.

Беда не в том, что краски две. Беда в том, что среди отставших оказались
ЛИСТЫ ПРИНЯТОГО: полная проверка знака, мелкий знак, алфавит и веса. То
есть лист, которым знак принимают, был нарисован не той краской, которой
знак печатают. Каждый модуль при этом честно проходил собственную
проверку — разойтись им ничто не мешало, и разойдясь они молчали.

Правило теперь одно

  Модуль, показывающий ПРИНЯТОЕ, берёт краски отсюда.
  Модуль, записавший разобранный перебор, остаётся на краске своего
  времени и объявляет себя в HISTORIC. Переписывать прошлые листы задним
  числом нельзя: они — запись того, что и на чём решалось.

Запуск: не запускается, только импортируется.
"""

import json
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from build import ROOT  # noqa: E402

with open(os.path.join(ROOT, "tools/premium.json"), encoding="utf-8") as f:
    _P = json.load(f)

PALETTE = dict(_P["palette"])
INK = PALETTE["ink"]
PAPER = PALETTE["paper"]
MUTED = PALETTE["muted"]
LINE = PALETTE["line"]
ACCENT = PALETTE["accent"]
# Волосок тоньше линейки: у palette_v2 он был отдельной краской, в
# принятой оснастке отдельного значения нет и заводить его не на чем.
HAIR = PALETTE["line"]

# Листы, которые сознательно остаются на краске своего времени. Сверка не
# считает их расхождением, но и молчать о них не должна: они перечислены
# здесь поимённо, а не спрятаны исключением в коде проверки.
HISTORIC = {
    "color": "перебор акцентов; его собственный синий отменён в color2",
    "color2": "пара акцентов синего мира, отменённого бордо",
    "ways": "девять исполнений синего мира",
    "approach": "восемь цветовых миров до выбора",
    "collab": "три исполнения коллаборации",
    "premium": "доводка оснастки: показывает «было» рядом со «стало»",
    "pair": "перебор срезов ляссе и геометрий уголка",
}
